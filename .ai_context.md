# AI Context — Friends & Family Billing

## Project Name
Friends & Family Billing (Family Bill Splitter)

## Project Type
Cloud-based web application on Firebase Hosting + Firestore + Cloud Functions + Firebase Auth.
**Two apps are deployed side-by-side** during an in-progress React migration.

## Deployed Apps

| URL | App | Stack | Status |
|-----|-----|-------|--------|
| `/` | Legacy app | Vanilla JS (esbuild) + Firebase CDN | **Primary** — fully functional |
| `/app/` | React app | React (Vite) + React Router + Firebase modular | **Secondary** — functional, pending visual review |

See `specs/ffb-react-migration.md` for migration plan and current state.

## Primary Languages
- JavaScript (Vanilla JS legacy + React)
- CSS3 (design tokens)

## Key Entry Points

| File | Purpose |
|------|---------|
| `src/main.js` | Legacy vanilla JS app (5,700 lines) — renders at `/` |
| `src/index.js` | Legacy window export bridge (464 lines) |
| `src/app/main.jsx` | React app entry point — renders at `/app/` |
| `src/app/App.jsx` | Root component — routing, auth, lazy-loaded views |
| `src/lib/BillingYearService.js` | Service-owns-state pattern — all billing mutations |
| `src/lib/firebase.js` | Modular Firebase init (reads from `window.__FIREBASE_CONFIG__`) |
| `firebase-config.js` | Legacy Firebase init (reads from `firebase-config.local.js`) |
| `functions/index.js` | Cloud Functions v2 — resolveShareToken, submitDispute, getEvidenceUrl, submitDisputeDecision |
| `AGENTS.md` | AI agent instructions and behavioral rules |

## Architecture

- **Dual-app coexistence**: Legacy JS at `/`, React SPA at `/app/`. Both share Firestore data and Firebase Auth.
- **Config bridge**: `firebase-config.local.js` sets `window.__FIREBASE_CONFIG__` — used by both apps. Gitignored, must exist locally and be deployed.
- **Service-owns-state**: `BillingYearService` owns canonical mutable state; React subscribes via `useSyncExternalStore`
- **Code-split**: Heavy views and dialogs loaded via `React.lazy()` + `<Suspense>`
- **Dual build**: `npm run build` runs `build:legacy` (esbuild → `script.js`) then `build:react` (Vite → `app/`)
- **Dual tests**: `npm test` runs `test:legacy` (287 tests) then `test:react` (489 tests) then `test:secrets`

## Directories

| Directory | Purpose |
|-----------|---------|
| `src/app/` | React application — components, views, hooks, contexts |
| `src/app/components/` | Shared React components (dialogs, badges, menus) |
| `src/app/views/` | Page-level React views (Dashboard, Manage, Settings, Share, Login) |
| `src/app/hooks/` | Custom hooks (useBillingData, useDisputes) |
| `src/app/contexts/` | React contexts (AuthContext, ToastContext) |
| `src/lib/` | Pure business logic — calculations, validation, formatting, services |
| `src/` | Legacy app source (main.js, index.js, platform/) |
| `functions/` | Cloud Functions v2 — deployed separately from hosting |
| `tests/` | Legacy VM-context test suite (billing.test.js) |
| `tests/react/` | Vitest + React Testing Library test suite |
| `specs/` | Feature specifications and acceptance criteria |
| `scripts/` | Automation scripts including secret scanning |

## External Dependencies

| Service | Purpose |
|---------|---------|
| Firebase Authentication | Email/Password and Google Sign-In |
| Cloud Firestore | NoSQL database — all billing data per user |
| Cloud Functions v2 | Share token resolution, dispute submission |
| Firebase Hosting | Static file hosting and CDN |
| Firebase Storage | Dispute evidence file uploads |
| Firebase Analytics | Usage event tracking |
| Vite | Build tool and dev server |

## Do Not Touch

| File/Path | Reason |
|-----------|--------|
| `firestore.rules` | Security rules — changes affect data access for all users |
| `storage.rules` | Storage security rules — same caution as above |
| `.env.local` | Real Firebase config — gitignored, never commit |

## High-Risk Areas (Require Human Review)

| Area | Risk |
|------|------|
| `src/lib/calculations.js` — `calculateAnnualSummary()` | Bill splitting math — errors affect real financial amounts |
| `src/lib/BillingYearService.js` — `recordPayment()` / `reversePayment()` | Ledger integrity — payments are append-only |
| `src/lib/calculations.js` — `getPaymentTotalForMember()` | Balance derivation — wrong totals mislead users |
| `firestore.rules` | Data access security |
| Cloud Functions auth/token validation | Public-facing endpoint security |
